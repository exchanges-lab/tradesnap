services:
  tradesnap:
    image: ghcr.io/exchanges-lab/tradesnap:latest
    container_name: tradesnap
    env_file:
      - .env
    ports:
      - "8003:8003"
    restart: unless-stopped
    # Optional: connect to an existing external network if needed
    # networks:
    #   - cycle

# networks:
#   cycle:
#     external: true
