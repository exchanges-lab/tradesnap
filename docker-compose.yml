services:
  tradesnap:
    image: ghcr.io/exchanges-lab/tradesnap:latest
    container_name: tradesnap
    env_file:
      - .env
    networks:
      - cycle
    restart: unless-stopped

networks:
  cycle:
    external: true
