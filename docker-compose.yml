services:
  tradesnap:
    image: ghcr.io/exchanges-lab/tradesnap:v1.0
    container_name: tradesnap
    env_file:
      - .env
    volumes:
      - ./target/chrome_profile:/app/target/chrome_profile
    networks:
      - cycle
    restart: unless-stopped

networks:
  cycle:
    external: true
